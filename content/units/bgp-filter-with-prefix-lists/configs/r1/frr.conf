! r1 - AS 65001  (the customer; pre-built, nothing to change here)
! r1 originates three prefixes toward r2. Two are its real allocations;
! one (10.0.0.0/8) is an RFC 1918 bogon it should never be advertising.
! The blackhole static routes give each prefix something to resolve against
! so BGP will actually advertise the matching `network` statements.
frr version 8.4
frr defaults traditional
hostname r1
log stdout
no ipv6 forwarding
!
interface lo
 ip address 1.1.1.1/32
!
interface eth1
 ip address 10.0.12.1/24
!
ip route 203.0.113.0/24 blackhole
ip route 198.51.100.0/24 blackhole
ip route 10.0.0.0/8 blackhole
!
router bgp 65001
 no bgp ebgp-requires-policy
 neighbor 10.0.12.2 remote-as 65002
 network 203.0.113.0/24
 network 198.51.100.0/24
 network 10.0.0.0/8
!
line vty
!
